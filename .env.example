# Nimbus Production Environment Variables
# Copy to .env and fill in values: cp .env.example .env

# ── PostgreSQL ───────────────────────────────────────────────────
POSTGRES_USER=nimbus
POSTGRES_PASSWORD=          # REQUIRED — strong password

# ── JWT ──────────────────────────────────────────────────────────
JWT_SECRET_KEY=             # REQUIRED — generate with: openssl rand -hex 32

# ── MinIO ────────────────────────────────────────────────────────
MINIO_ROOT_USER=nimbus
MINIO_ROOT_PASSWORD=        # REQUIRED — strong password

# ── SMTP (optional) ─────────────────────────────────────────────
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@nimbus.local

# ── Ports ────────────────────────────────────────────────────────
APP_PORT=80                 # Frontend (public)
TEMPORAL_UI_PORT=8233       # Temporal UI (admin only)
